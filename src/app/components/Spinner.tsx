import React from 'react';

const Spinner = () => {
  return (
    <h1>
      hello
      <p>je</p>
    </h1>
  );
};
{
  /* <div> */
}
{
  /* <If condition={props.isLoading}>
  <div className="spinner-overlay" data-test="spinner">
    <div className="spinner-container">
      <Spinner animation="border" variant="info" />
      <img
        src={require('../../styles/images/parsable-tower.png')}
        width="15"
        height="30"
        className="spinner-img d-inline-block align-top"
        alt="React Bootstrap logo"
      />
    </div>
  </div>
</If>
<div data-test="spinner-children" style={props.isLoading ? { display: 'none' } : {}}>
  {props.children}
</div>
</div>
); */
}
